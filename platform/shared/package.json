{
  "name": "@platform/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Общие утилиты и типы для всех сервисов",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pino": "^8.17.2",
    "pino-pretty": "^10.3.1",
    "zod": "^3.22.4",
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "@types/node": "^20.10.6",
    "typescript": "^5.5.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  }
}
